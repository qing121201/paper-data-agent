# 论文库数据目录

图形界面创建的论文库保存在这里。每个子目录对应一个独立论文库：

- `library.json`：论文库名称、ID 和更新时间。
- `papers.json`：论文标题、本地路径、原始网址、哈希和加入时间。
- `index.json`：按页分块后的本地检索索引。
- `downloaded_papers/`：从公开网页下载并缓存的 PDF。
- `sessions/`：这个论文库的 Agent 对话记录。

除本说明文件外，该目录内容被 Git 忽略。不要把包含私人论文或 API 凭据的数据提交到代码仓库。
